\section{实验设计与结果}
\label{sec:evaluation}


金融智能体的可靠性需要从回答质量、工具行为、证据支撑和运行效率等多个维度综合评价。FinTrace需要在多轮对话中识别公司、时间和任务，选择财务、文档、股权或事件工具，并将结构化结果组织为有证据约束的金融研判。任何一个环节发生错误，都可能引发主体错配、期间错配、数值错误、前视偏差或无证据结论。

本章采用“端到端智能体、确定性工具、专业报告”三层评测框架，将竞赛要求转化为\fintracekey{九项可复现指标}，并通过统一运行轨迹关联请求解析、工具调用、证据审查和答案生成。各类实验均按照“评价目的—指标来源与计算方法—实验结果—结果分析”的顺序展开，使指标选择、测量过程和业务结论保持对应。

\subsection{实验设置}

\subsubsection{评测任务与数据集}

多轮问答评测集由35个会话和1,410个轮次构成，每个会话包含12至85轮。问题覆盖事实查询、跨期比较、财务风险分析、股东关系、股权穿透、事件脉络、公告检索、研报观点及超出数据边界的问题。根据给定数据和工具能力，样本划分为可回答、不可回答和需要澄清三类。

所有会话均按照原始轮次顺序连续回放，依赖上下文的问题随完整会话一并评价。长上下文压力集在原始会话基础上扩展，使累计对话历史和可访问材料达到0.5M Tokens，并设置早期主体与指代、早期数值、跨轮期间约束和无关话题切换四类探针。实验同时记录数据集累计Token、各次大模型实际输入Token和记忆模块召回内容，以区分模型窗口、记忆压缩和检索策略的贡献。

专项工具在结构化结果层接受独立评价，具体做法是比较工具输出与领域人工参考标注。不同任务的样本单位和参考标注内容如下。

\begin{table}[htbp]
  \centering
  \caption{评测任务与参考标注}
  \label{tab:eval-tasks}
  \small
  \setlength{\tabcolsep}{4pt}
  \begin{tabular}{@{}C{0.18\textwidth}C{0.20\textwidth}L{0.54\textwidth}@{}}
    \toprule
    评测任务 & 样本单位 & 核心参考标注 \\
    \midrule
    多轮问答 & 轮次 / 会话 & 可回答性、核心事实、必要证据 \\
    工具路由 & 逻辑工具调用 & 可接受工具操作、必要性、关键参数、无需调用工具场景 \\
    错误处置与自纠错 & 实际运行轮次 & 错误类型、后续动作、最终状态和处置结论 \\
    股权穿透 & 自然语言查询案例 & 起止主体、观察日、逐跳节点、方向和比例 \\
    事件脉络 & 公司-时间窗案例 & 核心事件节点、类型、阶段、事件簇和来源 \\
    财务风险 & 公司-报告期-风险类型 & 正负标签、风险期间、指标证据和判定理由 \\
    报告盲评 & 财务排雷报告 & 专家维度评分、否决性错误和优秀标签 \\
    \bottomrule
  \end{tabular}
\end{table}

\subsubsection{模型与运行配置}

智能体的主回答模型与规划模型分别配置，以支持独立替换和对照实验。本次实验统一使用Qwen模型，所有结构化推理模块的温度设为0，以降低随机性。知识截止日由工作流统一注入，并在规划和工具执行阶段保持不变。

\begin{table}[htbp]
  \centering
  \caption{模型与运行配置}
  \label{tab:eval-config}
  \small
  \setlength{\tabcolsep}{4pt}
  \begin{tabular}{@{}C{0.25\textwidth}L{0.67\textwidth}@{}}
    \toprule
    配置项 & 实验配置 \\
    \midrule
    主回答模型 & \texttt{qwen3.7-plus-2026-05-26} \\
    规划模型 & \texttt{qwen3.7-plus-2026-05-26} \\
    API协议 & DashScope OpenAI-compatible API \\
    结构化推理温度 & \texttt{0.0} \\
    Python版本 & \texttt{3.12.13} \\
    知识截止日 & \texttt{2026-05-28} \\
    问题集 & 35个会话，1,410个轮次 \\
    文档索引 & \texttt{chunks-v2}，FTS5与FAISS混合检索 \\
    财务索引 & \texttt{financial-metrics-v1} \\
    股权索引 & \texttt{ownership-holdings-v3}、\texttt{entity-master-v1} \\
    事件索引 & \texttt{announcement-events-v3.1} \\
    研报观点索引 & \texttt{research-views-v1} \\
    运行轨迹 & \texttt{runtime/fintrace.sqlite3} \\
    \bottomrule
  \end{tabular}
\end{table}

每次正式运行设置唯一实验批次编号，并冻结源代码版本、依赖版本、模型、提示词版本、问题集哈希、数据清单、风险规则、实体映射、运行硬件和随机种子。配置发生变化时创建新的实验批次，以保证同一批次内结果可比。

\subsubsection{参考标注构建与质量控制}

问题主表保存问题、会话、轮次、主体、日期、可接受工具和必要文本片段。专项人工参考标注与问题主表分离，并通过案例编号关联。文本片段承担证据定位功能，原子事实承担答案内容判定功能；两者分别记录。参考标注依据冻结原始数据制作，与被评智能体及工具的输出保持独立。

正式参考标注采用双人独立标注和第三人仲裁。全量标注前先开展试标，统一实体、期间、单位、事件粒度和风险语义。评测仅纳入完成仲裁的标注。样本排除范围预先限定为重复记录、原始数据损坏或参考结论无法仲裁，并完整保留排除原因。

回答准确率采用金融标注者和冻结版本大模型评估器双重评价。两者一致时采用共同标签，不一致时由第二名金融标注者仲裁。大模型评估器先在不少于100个双人工样本上校准，并报告一致率和Cohen's κ系数；当κ低于0.70时，正式标签由双人工评价与仲裁产生。

\subsection{长上下文问答、工具调用与纠错}

长上下文实验首先评价系统能否在多轮交互中形成正确的最终回答。回答准确率用于衡量一轮回答是否整体可用，直接对应竞赛提出的“回答准确率”要求。该指标以轮次为单位，由金融标注者和经校准的大模型评估器依据人工参考标注共同判定。对于可回答问题，评价核心内容以及主体、期间、口径、数值和方向；对于不可回答问题，评价数据或能力边界说明和结论审慎性；对于需要澄清的问题，评价已给出的部分结果、缺失参数及其影响是否表达清楚。

主体或期间错配、核心数值或方向错误、使用截止日后信息、错误拒答、虚构证据，以及将风险信号表述为造假事实，均归为\fintracecaution{实质错误}。关键事实召回率（Recall）统计完成任务所必需的原子事实，命中要求主体、期间、指标口径和数值或方向全部正确。

仅使用回答准确率会掩盖“答案整体可接受但遗漏重要事实”的情况，因此同时采用关键事实召回率。该指标直接对应竞赛的关键事实召回要求，其分母来自每道问题经人工确认的必要原子事实集合，分子来自回答中得到正确表达且能够由证据支持的必要事实。所有轮次的事实命中数统一汇总后计算总体召回率，非必要背景信息不进入分母，避免答案篇幅影响结果。

据此，回答准确率与关键事实召回率分别定义为：

\[
A_{\mathrm{ans}}=\frac{N_{\mathrm{correct}}}{N_{\mathrm{turn}}},
\qquad
R_{\mathrm{fact}}=\frac{N_{\mathrm{hit}}}{N_{\mathrm{reference}}}.
\]

其中，$N_{\mathrm{correct}}$表示被判定为正确的回答轮次，$N_{\mathrm{turn}}$表示纳入评测的轮次总数；$N_{\mathrm{hit}}$表示回答正确覆盖的必要事实数量，$N_{\mathrm{reference}}$表示人工参考标注中的必要事实总数。$A_{\mathrm{ans}}$反映回答的整体可用性，$R_{\mathrm{fact}}$反映必要信息的覆盖程度，两者从整体判断和事实完整性两个角度评价长上下文问答质量。

工具调用精确率（Precision）用于衡量智能体发起的工具调用中有多少是必要且正确的。金融场景中的错误调用不仅增加延迟和成本，还可能把错误主体、错误期间或截止日后的数据带入回答，因此该指标直接回应竞赛对动态路由准确性的要求。评测以运行轨迹中的逻辑工具调用为单位，并与问题级人工参考标注中的可接受工具、操作和关键参数比对；同一调用标识内的物理重试只计算一次。工具、工具操作、调用必要性及公司、时间、指标、事件类型和截止日等关键参数全部正确时计为真正例（TP）；无关调用、重复调用、参数错误或无需调用工具时发生的调用计为假正例（FP）。

工具调用精确率按下式计算：

\[
P_{\mathrm{tool}}=\frac{TP_{\mathrm{tool}}}{TP_{\mathrm{tool}}+FP_{\mathrm{tool}}}.
\]

其中，$TP_{\mathrm{tool}}$为必要且工具、操作和关键参数均正确的逻辑调用数量，$FP_{\mathrm{tool}}$为无关、重复或关键参数错误的逻辑调用数量。该比值越高，说明智能体发起的调用越少偏离用户任务和数据边界。

错误处置与自纠错直接使用全量评测过程中真实产生的运行轨迹，不额外注入故障。工具执行失败、动作或参数校验失败、大模型结构化输出错误、网络异常以及工作流异常均记为运行错误。正确空结果、能力边界内的拒答和有证据依据的数据不足不属于运行错误；如果系统错误地选择工具、遗漏本可获得的数据或将异常结果继续用于回答，则仍按运行错误处理。

运行错误率用于观察系统在真实任务中的稳定性，定义为：

\[
R_{\mathrm{error}}=\frac{N_{\mathrm{error}}}{N_{\mathrm{all}}}.
\]

其中，$N_{\mathrm{all}}$为全部有效运行轮次，$N_{\mathrm{error}}$为至少出现一次运行错误的轮次。一个轮次内出现多项错误时仍只计一个错误轮次，避免连续错误重复放大结果。该指标越低，说明系统越少进入异常处理路径。

对于已经发生错误的轮次，进一步检查系统是否采取了与错误类型相符的后续动作。参数修复、重新规划、有界重试后完成任务，以及无法恢复时明确说明原因并停止生成无证据结论，均属于正确处置。错误处置成功率定义为：

\[
S_{\mathrm{handling}}=\frac{N_{\mathrm{handled}}}{N_{\mathrm{error}}}.
\]

其中，$N_{\mathrm{handled}}$为所有错误均得到正确处置的错误轮次。本文以$S_{\mathrm{handling}}$作为自纠错成功率的统计结果；它既考察系统能否从可修复错误中恢复，也考察面对不可恢复错误时能否安全终止。

为同时体现低错误率和错误发生后的处理能力，实验还报告\fintracemetric{总体可靠运行率}：

\[
R_{\mathrm{reliable}}=
\frac{N_{\mathrm{normal}}+N_{\mathrm{handled}}}{N_{\mathrm{all}}}.
\]

$N_{\mathrm{normal}}$表示未出现运行错误且正常结束的轮次。若整个评测批次未发生运行错误，则$R_{\mathrm{error}}$记为0，$R_{\mathrm{reliable}}$记为1，$S_{\mathrm{handling}}$因分母为0记为“不适用”。

依据上述口径，长上下文实验按Token档位报告性能变化，0.5M档用于核对竞赛要求，其他档位用于观察上下文增长是否引起持续性能衰减。表中同时记录端到端$P_{95}$延迟，以呈现不同上下文规模下的响应时间变化；回答准确率和关键事实召回率仍分别依据回答标签与必要事实命中情况计算。

延迟统一以秒为单位。端到端延迟从用户请求进入系统开始，到完整回答返回为止。$P_{50}$延迟是全部样本按耗时排序后的中位数，表示一半请求能够在该时间内完成；$P_{95}$延迟是第95百分位数，表示95\%的请求能够在该时间内完成，可用于观察少量慢请求对交互稳定性的影响；最大延迟则记录本次实验中耗时最长的单次请求。

\begin{table}[htbp]
  \centering
  \caption{不同上下文规模下的问答结果}
  \label{tab:context-results}
  \scriptsize
  \setlength{\tabcolsep}{2.5pt}
  \begin{tabular}{@{}C{0.19\textwidth}*{5}{C{0.13\textwidth}}@{}}
    \toprule
    Token档位 & 会话数 & 轮次数 & 回答准确率 & 关键事实召回率 & 端到端$P_{95}$延迟 \\
    \midrule
    10K & -- & -- & -- & -- & -- \\
    50K & -- & -- & -- & -- & -- \\
    100K & -- & -- & -- & -- & -- \\
    250K & -- & -- & -- & -- & -- \\
    500K & -- & -- & -- & -- & -- \\
    \bottomrule
  \end{tabular}
\end{table}

随着上下文规模由10K增加至500K Tokens，回答准确率由\_\_\_\_变化至\_\_\_\_，关键事实召回率由\_\_\_\_变化至\_\_\_\_；500K Tokens条件下两项指标分别为\_\_\_\_和\_\_\_\_。分档结果重点观察上下文增长后是否出现持续衰减，以及早期主体、期间和关键数值能否在后续轮次中被正确继承。结合现有工作流，错误更容易集中在跨话题切换后的指代消解、较早事实的重新召回和多项历史约束的同时满足，这些场景最能反映记忆筛选与上下文压缩策略的有效性。

工具调用结果按照工具领域统计。精确率用于呈现已发起调用的正确程度，工具召回率和参数准确率则用于进一步定位遗漏调用与参数错误。

\begin{table}[htbp]
  \centering
  \caption{分领域工具调用结果}
  \label{tab:tool-results}
  \scriptsize
  \setlength{\tabcolsep}{2.5pt}
  \begin{tabular}{@{}C{0.20\textwidth}C{0.15\textwidth}C{0.18\textwidth}C{0.18\textwidth}C{0.17\textwidth}@{}}
    \toprule
    工具领域 & 逻辑调用数 & 工具调用精确率 & 工具召回率 & 参数准确率 \\
    \midrule
    财务工具 & -- & -- & -- & -- \\
    股权工具 & -- & -- & -- & -- \\
    事件工具 & -- & -- & -- & -- \\
    文档与研报工具 & -- & -- & -- & -- \\
    \bottomrule
  \end{tabular}
\end{table}

工具调用实验共记录\_\_\_\_次逻辑调用，其中正确调用\_\_\_\_次、错误调用\_\_\_\_次，精确率为\_\_\_\_。从系统结构看，错误更可能出现在复合问题的能力拆分、隐含报告期的参数生成，以及事件类型与文档类型的区分，而主体代码和知识截止日由确定性模块校验，风险相对可控。

错误处置结果依据同一批次的运行轨迹统计，并按错误产生环节展开。只有该轮所有错误均得到妥善处理，才计入正确处置轮次。

\begin{table}[htbp]
  \centering
  \caption{错误处置结果}
  \label{tab:error-handling}
  \small
  \setlength{\tabcolsep}{4pt}
  \begin{tabular}{@{}C{0.25\textwidth}C{0.19\textwidth}C{0.22\textwidth}C{0.22\textwidth}@{}}
    \toprule
    错误类型 & 错误轮次数 & 正确处置轮次数 & 错误处置成功率 \\
    \midrule
    工具执行错误 & -- & -- & -- \\
    动作或参数校验错误 & -- & -- & -- \\
    大模型输出错误 & -- & -- & -- \\
    网络与基础设施错误 & -- & -- & -- \\
    总体 & -- & -- & -- \\
    \bottomrule
  \end{tabular}
\end{table}

全量评测共执行\_\_\_\_轮，其中\_\_\_\_轮出现运行错误，运行错误率为\_\_\_\_；\_\_\_\_个错误轮次得到正确处置，错误处置成功率为\_\_\_\_，总体可靠运行率为\_\_\_\_。结合现有实现，运行错误更可能集中在复杂动作的参数修复、大模型结构化输出和外部接口连接，未正确处置的案例也应围绕这些环节复核。若全量评测未出现运行错误，则错误处置成功率记为“不适用”，并结合运行错误率和总体可靠运行率说明系统稳定性。

为说明回答质量与实际运行代价之间的关系，端到端计时覆盖请求解析、规划、工具调用、证据审查和答案生成。平均Token是每轮主模型与规划模型输入、输出Token之和的算术平均值，平均成本则根据各模型的实际Token用量和调用价格折算。长上下文问答与工具调用的失败分别归入上下文继承、实体与时间解析、工具路由、参数生成、证据审查、答案生成和基础设施，以便将延迟或成本异常与具体工作流环节对应。

\begin{table}[htbp]
  \centering
  \caption{不同运行场景的效率与成本}
  \label{tab:runtime-cost}
  \scriptsize
  \setlength{\tabcolsep}{2.5pt}
  \begin{tabular}{@{}C{0.17\textwidth}*{6}{C{0.105\textwidth}}@{}}
    \toprule
    场景 & 样本数 & $P_{50}$延迟 & $P_{95}$延迟 & 最大延迟 & 平均Token & 平均成本 \\
    \midrule
    确定性直连 & -- & -- & -- & -- & -- & -- \\
    有界调查 & -- & -- & -- & -- & -- & -- \\
    多工具调查 & -- & -- & -- & -- & -- & -- \\
    证据不足或拒答 & -- & -- & -- & -- & -- & -- \\
    \bottomrule
  \end{tabular}
\end{table}

确定性直连、有界调查和多工具调查的$P_{95}$延迟分别为\_\_\_\_、\_\_\_\_和\_\_\_\_，平均成本分别为\_\_\_\_、\_\_\_\_和\_\_\_\_。多工具调查需要经历规划、证据复核和多次工具执行，耗时通常高于确定性直连；其中，本地SQLite查询和图搜索较快，主要等待时间来自规划模型、最终回答模型以及需要在线生成查询向量的文档检索。证据不足或拒答场景平均经过\_\_\_\_次工具调用后终止，共记录\_\_\_\_次无效循环。工作流设置了工具预算、重复动作校验和终止条件，可以限制无效循环；整体效率更容易受到外部模型接口网络延迟和响应稳定性的影响。

\subsection{股权穿透与事件脉络}

股权穿透的价值在于还原完整、可验证的控制或持股链条，任意一跳的主体、方向或比例错误都可能改变最终结论。因此，本实验采用案例级端到端准确率，而不以单个节点的局部正确代替整条路径的正确。该指标直接对应竞赛对深度大于3层路径的要求，评测范围从自然语言中的起点、终点和观察日解析开始，覆盖工具路由、实体桥接、图查询、比例计算和结果组装。参考路径由冻结股东快照和经确认的实体映射构建，并经人工复核。一个案例只有在起止主体、观察日、路径方向、每一跳节点、每一跳比例、累计比例和必要路径全部正确，且不存在无证据额外路径时才通过。

设有效穿透案例总数为$N_{\mathrm{path}}$，其中满足全部严格条件的案例数为$N_{\mathrm{strict}}$，则股权穿透端到端准确率为：

\[
A_{\mathrm{ownership}}=\frac{N_{\mathrm{strict}}}{N_{\mathrm{path}}}.
\]

$N_{\mathrm{path}}$仅包含路径深度大于3且具备完整人工参考路径的案例。对于一条包含$L$跳的持股路径，其累计持股比例按每一跳比例$r_l$连乘计算：

\[
r_{\mathrm{path}}=\prod_{l=1}^{L}r_l.
\]

路径中的节点、方向或任一$r_l$错误，都会使该案例无法计入$N_{\mathrm{strict}}$；这与股权穿透结论必须逐跳成立的业务要求一致。

事件脉络任务的首要目标是避免遗漏影响研判的关键节点，因此选用事件节点召回率作为竞赛验收指标。样本以公司、时间窗和事件类型的组合为单位，参考节点由冻结公告中的事件日期、主体、类型和阶段形成，并由人工确认其重要性。系统输出与参考节点在主体、事件类型、发生日期和核心事项上相符时记为命中；同一事件的重复表述只计一次。否定性历史描述与已发生事件分别处理，因果关系仅在来源文本明确支持时纳入评价。

事件节点召回率和精确率分别定义为：

\[
R_{\mathrm{event}}=\frac{N_{\mathrm{event\_hit}}}{N_{\mathrm{event\_reference}}},
\qquad
P_{\mathrm{event}}=\frac{N_{\mathrm{event\_hit}}}{N_{\mathrm{event\_output}}}.
\]

其中，$N_{\mathrm{event\_hit}}$表示与人工参考节点成功匹配的事件数，$N_{\mathrm{event\_reference}}$表示核心参考事件总数，$N_{\mathrm{event\_output}}$表示系统输出的去重事件总数。$R_{\mathrm{event}}$衡量关键事件是否遗漏，$P_{\mathrm{event}}$用于识别无关或错误事件是否被加入时间线。

节点召回率能够反映事件是否完整，却无法单独评价事件簇的边界。因此，实验同时检查聚类成对F1、日期顺序、知识截止日、事件阶段一致性和显式因果关系的来源支持。聚类评价将任意两个事件视为一个事件对：系统与人工参考标注均将其归入同一事件簇时计为真正例，据此得到事件对精确率$P_{\mathrm{pair}}$和召回率$R_{\mathrm{pair}}$，并计算：

\[
F1_{\mathrm{pair}}=\frac{2P_{\mathrm{pair}}R_{\mathrm{pair}}}{P_{\mathrm{pair}}+R_{\mathrm{pair}}}.
\]

该指标同时惩罚错误合并和错误拆分。事件先后顺序与来源证据相反、将后续事件写为前因、混淆事件阶段或使用截止日后信息，均视为明显逻辑断裂，并在结果中单独统计。

工具执行效率从工具接收到已校验参数开始计时，到完整结构化结果返回为止，包含数据库查询、图搜索、计算和结果组装，不包含规划、最终答案生成和前端渲染。各工具操作按有结果与空结果、短窗与长窗、浅路径与深路径分层，每类在预热后至少运行30次。$P_{50}$、$P_{95}$和最大值沿用前述定义，其中最大值反映本次实验中最慢的单次工具调用。

实验结果按照任务难度分组呈现。股权样本按路径深度划分，事件样本区分事件查询与事件聚类，并同时给出核心指标、分项正确性和延迟分布。

\begin{table}[htbp]
  \centering
  \caption{股权穿透与事件脉络专项结果}
  \label{tab:ownership-event-results}
  \scriptsize
  \setlength{\tabcolsep}{2.5pt}
  \begin{tabular}{@{}C{0.13\textwidth}*{7}{C{0.105\textwidth}}@{}}
    \toprule
    专项 & 样本数 & 核心指标 & 节点/边准确率 & 比例/聚类结果 & $P_{50}$ & $P_{95}$ & 最大值 \\
    \midrule
    深度4股权路径 & -- & -- & -- & -- & -- & -- & -- \\
    深度5及以上股权路径 & -- & -- & -- & -- & -- & -- & -- \\
    事件查询 & -- & -- & -- & -- & -- & -- & -- \\
    事件聚类 & -- & -- & -- & -- & -- & -- & -- \\
    \bottomrule
  \end{tabular}
\end{table}

深度大于3层的股权穿透案例共\_\_\_\_个，严格通过\_\_\_\_个，端到端准确率为\_\_\_\_。错误首次出现的中位跳数为\_\_\_\_，潜在原因主要包括股东名称无法桥接到唯一上市公司主体、观察日缺少有效快照，以及前十大股东数据未覆盖完整控制链。事件任务共包含\_\_\_\_个核心节点，系统正确识别\_\_\_\_个，节点召回率为\_\_\_\_；事件精确率和聚类成对F1分别为\_\_\_\_和\_\_\_\_。严重时序断裂、截止日违规和无证据因果关系分别出现\_\_\_\_、\_\_\_\_和\_\_\_\_次。股权与事件工具的最大执行时间分别为\_\_\_\_和\_\_\_\_。两项任务的潜在误差主要来自实体桥接、公告日期代替精确发生日期，以及规则抽取对事件类型和事件阶段的区分不足，而不是图搜索或时间排序算法本身。

\subsection{财务风险预警与报告质量}

财务风险样本中，未触发风险的情形通常多于触发风险的情形，单独使用准确率容易被多数类掩盖。为同时约束误报与漏报，本实验采用F1作为风险预警的核心指标，并分别报告精确率和召回率。样本以“公司、目标报告期、风险类型”为最小单位，人工参考标签由金融标注者依据冻结财务记录和截止日前证据判定，只描述可观察的风险信号，不将规则命中直接解释为财务造假。

财务风险预警的精确率、召回率和F1定义为：

\[
P_{\mathrm{risk}}=\frac{TP_{\mathrm{risk}}}{TP_{\mathrm{risk}}+FP_{\mathrm{risk}}},
\qquad
R_{\mathrm{risk}}=\frac{TP_{\mathrm{risk}}}{TP_{\mathrm{risk}}+FN_{\mathrm{risk}}},
\]

\[
F1_{\mathrm{risk}}=\frac{2P_{\mathrm{risk}}R_{\mathrm{risk}}}{P_{\mathrm{risk}}+R_{\mathrm{risk}}}.
\]

其中，$TP_{\mathrm{risk}}$表示系统与人工参考标签均判定存在该类风险信号的样本数，$FP_{\mathrm{risk}}$表示系统触发但人工参考标签为负的样本数，$FN_{\mathrm{risk}}$表示人工参考标签为正但系统未触发的样本数。精确率反映误报程度，召回率反映漏报程度，F1是二者的调和平均值；只有同时减少误报和漏报，F1才会提高。

真正例、假正例和假负例由工具输出与人工参考标签逐项比对获得。数据不足和规则不适用样本单独核验状态识别，不进入风险正负样本分母；数据足以形成正向参考标签而系统未覆盖相应风险类型时计为假负例（FN）。正式结果采用冻结风险类型全集上的微平均F1，使每个样本对总体结果具有相同权重；各风险类型的精确率、召回率和F1用于呈现不同规则的性能差异。

规则命中正确并不意味着最终报告具有实际使用价值，报告还需要准确引用数据、解释指标关系并保持审慎的风险措辞。因此，财务排雷报告采用专家盲评，与确定性规则的F1形成互补。两名具备财务、审计或证券研究背景的专家分别从数据与引用、逻辑自洽、金融专业性、完整与可用性四个维度评分，每项采用1至5分。单项差异超过1分或优秀结论不一致时，由第三名专家仲裁。四个维度均不低于4分、平均分不低于4.0，且不存在核心事实错误、虚假引用、截止日泄露或直接定性造假时，报告判定为优秀。

在完成评分和必要仲裁后，专家盲评优秀率按下式计算：

\[
E_{\mathrm{report}}=\frac{N_{\mathrm{excellent}}}{N_{\mathrm{valid}}}.
\]

其中，$N_{\mathrm{valid}}$为完成盲评且不存在样本损坏等排除情形的报告总数，$N_{\mathrm{excellent}}$为同时满足四项评分门槛且不存在否决性错误的报告数。该指标将多维评分转化为报告级验收结果，避免单项高分掩盖核心事实错误或虚假引用。

财务风险结果按风险类型分别报告正负样本规模、精确率、召回率和F1，并使用微平均结果反映整体预警能力。报告质量通过专家评分均值、评分离散程度、优秀占比和否决性错误数量进行评价。

\begin{table}[htbp]
  \centering
  \caption{分类型财务风险识别结果}
  \label{tab:risk-results}
  \scriptsize
  \setlength{\tabcolsep}{2.5pt}
  \begin{tabular}{@{}C{0.19\textwidth}*{5}{C{0.13\textwidth}}@{}}
    \toprule
    风险类型 & 正样本 & 负样本 & 精确率 & 召回率 & F1 \\
    \midrule
    利润与经营现金流背离 & -- & -- & -- & -- & -- \\
    存货与收入背离 & -- & -- & -- & -- & -- \\
    应收账款与收入背离 & -- & -- & -- & -- & -- \\
    经营现金流持续为负 & -- & -- & -- & -- & -- \\
    销售收现与收入背离 & -- & -- & -- & -- & -- \\
    利润率异常变化 & -- & -- & -- & -- & -- \\
    流动性与杠杆压力 & -- & -- & -- & -- & -- \\
    总体微平均 & -- & -- & -- & -- & -- \\
    \bottomrule
  \end{tabular}
\end{table}

风险预警实验共纳入\_\_\_\_个有效样本，其中正样本\_\_\_\_个、负样本\_\_\_\_个。总体精确率、召回率和微平均F1分别为\_\_\_\_、\_\_\_\_和\_\_\_\_。分类型结果需要结合指标可得性解释：经营现金流持续为负等单期规则所需字段较少，执行条件相对稳定；利润与现金流、存货与收入、应收账款与收入等跨期规则依赖连续可比期间，缺失前期数据时容易形成漏检；利润率、流动性和杠杆规则则更容易受到行业差异及初始阈值的影响。因此，各类风险表现的差异主要反映数据完整性、期间可比性和阈值适用范围，而不只是规则计算是否正确。

在风险识别结果的基础上，专家盲评进一步检验报告能否把指标、证据和风险解释组织成完整研判。

\begin{table}[htbp]
  \centering
  \caption{财务排雷报告专家盲评结果}
  \label{tab:report-review}
  \scriptsize
  \setlength{\tabcolsep}{2.5pt}
  \begin{tabular}{@{}C{0.20\textwidth}C{0.15\textwidth}C{0.18\textwidth}C{0.18\textwidth}C{0.17\textwidth}@{}}
    \toprule
    专家评分维度 & 平均分 & 标准差 & 4分及以上占比 & 否决性错误数量 \\
    \midrule
    数据与引用 & -- & -- & -- & -- \\
    逻辑自洽 & -- & -- & -- & -- \\
    金融专业性 & -- & -- & -- & -- \\
    完整与可用性 & -- & -- & -- & -- \\
    总体优秀率 & -- & -- & -- & -- \\
    \bottomrule
  \end{tabular}
\end{table}

有效盲评报告共\_\_\_\_份，其中优秀报告\_\_\_\_份，优秀率为\_\_\_\_。四个维度的具体得分及排序由盲评结果确定，共发现\_\_\_\_项否决性错误。报告生成受到结构化工具证据约束，数据引用和风险信号溯源具有较清晰的核验路径；相对薄弱的部分更可能是跨工具证据的综合解释、行业背景缺失时的专业判断，以及对复杂风险成因的完整说明。否决性错误重点检查核心数值或期间错误、引用无法回溯、超出知识截止日和将风险信号直接定性为造假。

财务专项的运行效率分为两个环节记录。风险扫描是确定性计算过程，统计数据查询与规则计算耗时；报告生成依赖大模型，除延迟外还记录输入输出Token和调用成本。失败原因按指标数据、报告期口径、风险规则、证据引用和报告生成分类，用于解释异常耗时和报告质量变化。

\begin{table}[htbp]
  \centering
  \caption{财务风险分析效率与成本}
  \label{tab:risk-cost}
  \scriptsize
  \setlength{\tabcolsep}{2.5pt}
  \begin{tabular}{@{}C{0.17\textwidth}*{6}{C{0.105\textwidth}}@{}}
    \toprule
    环节 & 样本数 & $P_{50}$延迟 & $P_{95}$延迟 & 最大延迟 & 平均Token & 平均成本 \\
    \midrule
    风险扫描 & -- & -- & -- & -- & -- & -- \\
    财务报告生成 & -- & -- & -- & -- & -- & -- \\
    \bottomrule
  \end{tabular}
\end{table}

风险扫描和财务报告生成的$P_{95}$延迟分别为\_\_\_\_和\_\_\_\_。报告平均消耗\_\_\_\_ Tokens，单份平均成本为\_\_\_\_。风险扫描在本地完成数据查询和规则计算，通常耗时较低且波动较小；报告生成需要组织多条证据并调用主模型，是财务专项更主要的延迟与成本来源。报告优秀率与单位生成成本结合考察，可以判断增加证据数量和回答长度是否带来了相应的质量提升。

\subsection{综合分析与本章小结}

表中仅汇总竞赛技术要求明确规定的验收结果。各项数值沿用前述实验的计算口径，不在汇总阶段重新加权或调整。

\begin{table}[htbp]
  \centering
  \caption{竞赛技术要求综合验收结果}
  \label{tab:acceptance-results}
  \small
  \setlength{\tabcolsep}{4pt}
  \begin{tabular}{@{}C{0.25\textwidth}C{0.19\textwidth}C{0.22\textwidth}C{0.22\textwidth}@{}}
    \toprule
    竞赛技术要求 & 实验结果 & 验收目标 & 验收结论 \\
    \midrule
    0.5M Tokens、10轮以上多轮问答的回答准确率 & -- & ≥90\% & -- \\
    0.5M Tokens、10轮以上多轮问答的关键事实召回率 & -- & ≥90\% & -- \\
    外部工具调用精确率 & -- & ≥92\% & -- \\
    真实运行错误的正确处置率（自纠错成功率） & -- & ≥80\% & -- \\
    深度大于3层的股权穿透端到端准确率 & -- & ≥85\% & -- \\
    事件关键节点召回率及因果、时序一致性 & -- & 召回率≥85\%，无明显逻辑断裂 & -- \\
    股权穿透与事件脉络工具执行时间 & -- & ≤5秒 & -- \\
    财务风险预警微平均F1 & -- & ≥85\% & -- \\
    财务排雷报告专家盲评优秀率 & -- & ≥80\% & -- \\
    \bottomrule
  \end{tabular}
\end{table}

九项技术要求中共有\_\_\_\_项达到验收目标。0.5M Tokens长上下文实验的回答准确率和关键事实召回率分别为\_\_\_\_和\_\_\_\_。系统采用分层记忆和确定性实体、时间解析机制，有助于保持主体、报告期和已确认关键事实；远距离指代、话题切换后的上下文取舍和多项历史约束并存仍是风险较高的场景。工具调用精确率为\_\_\_\_，运行错误率、错误处置成功率和总体可靠运行率分别为\_\_\_\_、\_\_\_\_和\_\_\_\_。确定性直连和参数完整的单工具请求具有清晰的执行路径，复杂调查中的能力拆分、动作修复以及外部模型连续失败则更可能影响路由与错误处置结果。

股权穿透端到端准确率为\_\_\_\_，事件关键节点召回率为\_\_\_\_，股权与事件工具的实测执行时间分别为\_\_\_\_和\_\_\_\_。确定性图搜索、比例连乘和事件时间排序的行为较为明确，专项误差更可能来自图查询之前的实体桥接，以及公告事件的类型、阶段和精确日期抽取。这一结论需要结合数据覆盖范围理解：股权关系来自前十大股东快照和经确认的主体桥接，因此只评价现有数据能够证明的持股链条；事件节点主要来自公告，事件顺序和因果关系也以公告中的明确表述为准。在这一边界内，专项实验既检验了自然语言到工具结果的完整链路，也反映了实体映射和来源数据对推理深度的实际约束。

财务风险预警的微平均F1为\_\_\_\_，专家盲评优秀率为\_\_\_\_。具备直接结构化字段且不依赖行业阈值的风险规则具有更稳定的执行条件，依赖连续期间、行业基准或当前数据未覆盖科目的风险识别则更容易受到限制。现有风险规则主要依据三大报结构化指标识别跨科目背离和连续性异常，对异常关联交易、异常费用和汇兑的覆盖相对有限。因此，系统能够在现有数据范围内提供可回溯的风险信号、计算过程和审慎研判，但不能将风险信号直接解释为财务造假事实。

总体来看，FinTrace在端到端回答、确定性工具和专业报告三个层面形成了相互衔接的评价结果。0.5M上下文规模由实际Token用量确认，回答事实同时核对来源证据和最终表达，风险预警基于独立正负样本计算，工具性能通过分层重复测试获得。正确空结果和有依据的数据不足不作为运行错误，真实错误则根据后续动作区分为正确处置与未正确处置。由此得到的实验结果既可用于判断竞赛技术要求的完成情况，也能够说明性能差异来自上下文管理、工具执行、数据覆盖还是报告生成环节。
