\section{实验设计与结果}
\label{sec:evaluation}


金融智能体的可靠性需要从回答质量、工具行为、证据支撑和运行效率等多个维度综合评价。FinTrace需要在多轮对话中识别公司、时间和任务，选择财务、文档、股权或事件工具，并将结构化结果组织为有证据约束的金融研判。任何一个环节发生错误，都可能引发主体错配、期间错配、数值错误、前视偏差或无证据结论。

本章采用“端到端智能体、确定性工具、专业报告”三层评测框架，将竞赛要求转化为\fintracekey{九项可复现指标}，并通过统一运行轨迹关联请求解析、工具调用、证据审查和答案生成。各类实验均按照“评价目的—指标来源与计算方法—实验结果—结果分析”的顺序展开，使指标选择、测量过程和业务结论保持对应。

\subsection{实验设置}

\subsubsection{评测任务与数据集}

多轮问答评测集由35个会话和1,410个轮次构成，每个会话包含12至85轮。问题覆盖事实查询、跨期比较、财务风险分析、股东关系、股权穿透、事件脉络、公告检索、研报观点及超出数据边界的问题。根据给定数据和工具能力，样本划分为可回答、不可回答和需要澄清三类。

所有会话均按照原始轮次顺序连续回放，依赖上下文的问题随完整会话一并评价。长上下文压力集在原始会话基础上扩展，使累计对话历史和可访问材料达到0.5M Tokens，并设置早期主体与指代、早期数值、跨轮期间约束和无关话题切换四类探针。实验同时记录数据集累计Token、各次大模型实际输入Token和记忆模块召回内容，以区分模型窗口、记忆压缩和检索策略的贡献。

专项工具在结构化结果层接受独立评价，具体做法是比较工具输出与独立构建的参考标注或复核结论。不同任务的样本单位和参考内容如下。

\begin{table}[htbp]
  \centering
  \caption{评测任务与参考标注}
  \label{tab:eval-tasks}
  \small
  \setlength{\tabcolsep}{4pt}
  \begin{tabular}{@{}C{.18\textwidth}C{.23\textwidth}C{.51\textwidth}@{}}
    \fintracetoprule
    \fintracetablehead{评测任务} & \fintracetablehead{样本单位} & \fintracetablehead{核心参考标注} \\
    \fintracemidrule
    多轮问答 & 轮次 / 会话 & 可回答性、核心事实、必要证据 \\
    工具路由 & 逻辑工具调用 & 可接受工具操作、必要性、关键参数、无需调用工具场景 \\
    错误处置与自纠错 & 实际运行轮次 & 错误类型、后续动作、最终状态和处置结论 \\
    股权穿透 & 自然语言查询案例 & 起止主体、观察日、逐跳节点、方向和比例 \\
    事件脉络 & 公司-时间窗案例 & 核心事件节点、类型、阶段、事件簇和来源 \\
    财务风险 & 公司-报告期-风险类型 & 正负标签、风险期间、指标证据和判定理由 \\
    报告盲评 & 财务排雷报告 & 专家维度评分、否决性错误和优秀标签 \\
    \fintracebottomrule
  \end{tabular}
\end{table}

\subsubsection{模型与运行配置}

智能体的主回答模型与规划模型分别配置，以支持独立替换和对照实验。本次实验统一使用Qwen模型，所有结构化推理模块的温度设为0，以降低随机性。知识截止日由工作流统一注入，并在规划和工具执行阶段保持不变。

\begin{table}[htbp]
  \centering
  \caption{模型与运行配置}
  \label{tab:eval-config}
  \small
  \setlength{\tabcolsep}{4pt}
  \begin{tabular}{@{}C{.25\textwidth}C{.67\textwidth}@{}}
    \fintracetoprule
    \fintracetablehead{配置项} & \fintracetablehead{实验配置} \\
    \fintracemidrule
    主回答模型 & \texttt{qwen3.7-plus} \\
    规划模型 & \texttt{qwen3.7-plus} \\
    API协议 & DashScope OpenAI-compatible API \\
    结构化推理温度 & \texttt{0.0} \\
    Python版本 & \texttt{3.12.13} \\
    知识截止日 & \texttt{2026-05-28} \\
    问题集 & 35个会话，1,410个轮次 \\
    实验批次 & \texttt{EVAL-20260825T204302Z-FA59F317-2CB3} \\
    \fintracebottomrule
  \end{tabular}
\end{table}

每次正式运行设置唯一实验批次编号，并冻结源代码版本、依赖版本、模型、提示词版本、问题集哈希、数据清单、风险规则、实体映射、运行硬件和随机种子。配置发生变化时创建新的实验批次，以保证同一批次内结果可比。

\subsubsection{参考标注构建与质量控制}

问题主表保存问题、会话、轮次、主体、日期、可接受工具和必要文本片段。专项参考结果与问题主表分离，并通过案例编号关联。文本片段承担证据定位功能，原子事实承担答案内容判定功能；两者分别记录。参考结果依据冻结原始数据制作，与被评智能体及工具的输出保持独立，并记录生成方式和版本。

正式参考标注采用双人独立标注和第三人仲裁。全量标注前先开展试标，统一实体、期间、单位、事件粒度和风险语义。评测仅纳入完成仲裁的标注。样本排除范围预先限定为重复记录、原始数据损坏或参考结论无法仲裁，并完整保留排除原因。

回答准确率的完整实验设计采用金融标注者和冻结版本大模型评估器双重评价。当前全量结果由冻结版本大模型评估器逐轮审查，并经项目负责人确认后作为代理人工复核结果写入评测表；原始模型标签、映射记录和修改前文件均单独保留。该结果可用于现阶段回答质量统计，但\fintracecaution{不等同于独立金融专家复核}，后续专家抽查时应优先覆盖错误案例、复杂财务调查和上下文切换案例。

\subsection{长上下文问答、工具调用与纠错}

\subsubsection{回答质量与工具路由评价方法}

长上下文实验首先评价系统能否在多轮交互中形成正确的最终回答。回答准确率用于衡量一轮回答是否整体可用，直接对应竞赛提出的“回答准确率”要求。该指标以轮次为单位，依据统一审核标准检查最终回答。对于可回答问题，评价核心内容以及主体、期间、口径、数值和方向；对于不可回答问题，评价数据或能力边界说明和结论审慎性；对于需要澄清的问题，评价已给出的部分结果、缺失参数及其影响是否表达清楚。

主体或期间错配、核心数值或方向错误、使用截止日后信息、错误拒答、虚构证据，以及将风险信号表述为造假事实，均归为实质错误。关键事实召回率（Recall）统计完成任务所必需的原子事实，命中要求主体、期间、指标口径和数值或方向全部正确。

仅使用回答准确率会掩盖“答案整体可接受但遗漏重要事实”的情况，因此同时采用关键事实召回率。该指标直接对应竞赛的关键事实召回要求，其分母来自每道问题经复核确认的必要原子事实集合，分子来自回答中得到正确表达且能够由证据支持的必要事实。所有轮次的事实命中数统一汇总后计算总体召回率，非必要背景信息不进入分母，避免答案篇幅影响结果。

据此，回答准确率与关键事实召回率分别定义为：

\[
A_{\mathrm{ans}}=\frac{N_{\mathrm{correct}}}{N_{\mathrm{turn}}},
\qquad
R_{\mathrm{fact}}=\frac{N_{\mathrm{hit}}}{N_{\mathrm{reference}}}.
\]

其中，$N_{\mathrm{correct}}$表示被判定为正确的回答轮次，$N_{\mathrm{turn}}$表示纳入评测的轮次总数；$N_{\mathrm{hit}}$表示回答正确覆盖的必要事实数量，$N_{\mathrm{reference}}$表示人工参考标注中的必要事实总数。$A_{\mathrm{ans}}$反映回答的整体可用性，$R_{\mathrm{fact}}$反映必要信息的覆盖程度，两者从整体判断和事实完整性两个角度评价长上下文问答质量。

工具调用评价使用必要调用正确率和轮次级路由正确率。评测以运行轨迹中的逻辑工具调用为单位，同一调用标识内的物理重试只计算一次。对于经复核确认为回答问题所必需的调用，只有工具、操作和主体、时间、指标、事件类型与知识截止日等关键参数全部正确，才计为正确的必要调用。额外调用不进入该指标的分母。

设必要调用总数为$N_{\mathrm{needed}}$，其中工具、操作和关键参数均正确的调用数为$N_{\mathrm{needed\_correct}}$，则必要调用正确率为：

\[
A_{\mathrm{needed}}=\frac{N_{\mathrm{needed\_correct}}}{N_{\mathrm{needed}}}.
\]

轮次级路由正确率以完整对话轮次为单位。一轮中所有必要调用均正确且没有遗漏必要工具时，该轮计为路由正确。设路由正确轮次数为$N_{\mathrm{route\_correct}}$，总轮次数为$N_{\mathrm{turn}}$，则：

\[
A_{\mathrm{route}}=\frac{N_{\mathrm{route\_correct}}}{N_{\mathrm{turn}}}.
\]

\subsubsection{错误处置与可靠性评价方法}

错误处置与自纠错直接使用全量评测过程中真实产生的运行轨迹，不额外注入故障。系统根据结构化运行日志自动识别工具执行失败、动作或参数校验失败、大模型结构化输出错误、网络异常和工作流异常。正确空结果、能力边界内的拒答和有证据依据的数据不足不会产生错误记录，因而不进入该指标。工具路由或回答内容是否正确分别由必要调用正确率、轮次级路由正确率和回答准确率评价，避免在错误处置指标中重复计算。

运行错误率用于观察系统在真实任务中的稳定性，定义为：

\[
R_{\mathrm{error}}=\frac{N_{\mathrm{error}}}{N_{\mathrm{all}}}.
\]

其中，$N_{\mathrm{all}}$为全部有效运行轮次，$N_{\mathrm{error}}$为运行轨迹中至少出现一次可检测错误的轮次。一个轮次内出现多项错误时仍只计一个错误轮次，分类统计允许同一轮同时属于多个错误类型，因此分类数量之和可能大于错误轮次总数。该指标越低，说明系统越少进入异常处理路径。

对于已经发生错误的轮次，系统继续检查最终一次Agent运行是否在同一运行内部处理了该轮涉及的全部错误类型，并以非失败状态安全结束。参数修复、重新规划、有界重试后完成任务，以及无法继续时停止生成无证据结论，均属于正确处置；仅由评测脚本在之后重新执行成功的轮次不计为Agent自动纠错成功。错误处置成功率定义为：

\[
S_{\mathrm{handling}}=\frac{N_{\mathrm{handled}}}{N_{\mathrm{error}}}.
\]

其中，$N_{\mathrm{handled}}$为全部可检测错误类型均在最终一次Agent运行内得到处理的错误轮次。本文以$S_{\mathrm{handling}}$作为自纠错成功率的统计结果。回答内容是否正确不由该指标重复判断，而由回答准确率单独反映。

为同时体现低错误率和错误发生后的处理能力，实验还报告\fintracekey{总体可靠运行率}：

\[
R_{\mathrm{reliable}}=
\frac{N_{\mathrm{normal}}+N_{\mathrm{handled}}}{N_{\mathrm{all}}}.
\]

$N_{\mathrm{normal}}$表示未出现运行错误且正常结束的轮次。若整个评测批次未发生运行错误，则$R_{\mathrm{error}}$记为0，$R_{\mathrm{reliable}}$记为1；由于此时不存在需要处置的错误，$S_{\mathrm{handling}}$不再计算。

\subsubsection{长上下文、路由与纠错实验结果}

依据上述口径，长上下文实验按Token档位报告性能变化，0.5M档用于核对竞赛要求，其他档位用于观察上下文增长是否引起持续性能衰减。表中同时记录端到端$\mathrm{P}_{95}$延迟，以呈现不同上下文规模下的响应时间变化；回答准确率和关键事实召回率仍分别依据回答标签与必要事实命中情况计算。

延迟统一以秒为单位。端到端延迟从用户请求进入系统开始，到完整回答返回为止。$\mathrm{P}_{50}$延迟是全部样本按耗时排序后的中位数，表示一半请求能够在该时间内完成；$\mathrm{P}_{95}$延迟是第95百分位数，表示95\%的请求能够在该时间内完成，可用于观察少量慢请求对交互稳定性的影响；最大延迟则记录本次实验中耗时最长的单次请求。

\begin{table}[htbp]
  \centering
  \caption{不同上下文规模下的问答结果}
  \label{tab:context-results}
  \footnotesize
  \setlength{\tabcolsep}{2.5pt}
  \begin{tabular}{@{}C{.24\textwidth}*{5}{C{.13\textwidth}}@{}}
    \fintracetoprule
    \fintracetablehead{Token档位} & \fintracetablehead{会话数} & \fintracetablehead{轮次数} & \fintracetablehead{回答准确率} & \fintracetablehead{关键事实召回率} & \fintracetablehead{端到端$\mathrm{P}_{95}$延迟} \\
    \fintracemidrule
    10K & 35 & 1,111 & 96.04\% & 92.37\% & 42.60秒 \\
    50K & 32 & 249 & 91.97\% & 96.64\% & 225.27秒 \\
    100K & 17 & 48 & 89.58\% & 92.98\% & 489.66秒 \\
    250K & 2 & 2 & 100.00\% & 100.00\% & 646.19秒 \\
    500K & 0 & 0 & / & / & / \\
    \fintracebottomrule
  \end{tabular}
\end{table}

本表按照每轮工作流内全部大模型调用的输入与输出Token总量分档，各档依次表示不超过10K、10K至50K、50K至100K、100K至250K和250K至500K。同一会话中的不同轮次可能落入不同档位，因此各档会话数不进行加总。本批次单轮最高Token总量为116,283，未出现250K至500K样本，500K档的“0”表示实际运行未形成该规模的模型输入，而非该档回答准确率为0。

未形成0.5M Tokens输入与系统的上下文治理方式有关。FinTrace不将全部历史消息、公告、研报和工具结果持续拼接到提示词中，而是先对原始文档进行结构化清洗和自然段切分，在线阶段只检索与当前主体和任务相关的少量Chunk；多轮对话则保留近期消息，并将较早内容压缩为会话摘要和带证据标识的已验证事实。结构化财务、股权和事件结果也以受限证据集合传入模型，文档Chunk设置独立数量上限，避免长篇文本挤占关键数值和关系证据。该机制减少了无关历史信息、重复原文和低相关检索结果造成的上下文膨胀，使大部分边界判断在调用模型前完成，也降低了长上下文中的注意力稀释、早期事实遗失、推理延迟和调用成本。因此，现有评测更接近经过检索、摘要和记忆压缩后的实际Agent负载，而不是人为累积全部原始内容得到的窗口上限测试。需要说明的是，这一结果证明了系统能够主动控制上下文规模，但\fintracecaution{不能替代}0.5M Tokens专项压力实验；若按照竞赛口径验证极限输入条件，仍需另行构造保留关键事实的超长会话进行测试。

全量1,410轮回答中，1,341轮通过审核，\fintracemetric{回答准确率为95.11\%}；248轮具备可核验的必要事实，共标注1,160项，正确命中1,103项，\fintracemetric{关键事实召回率为95.09\%}。10K、50K和100K档的回答准确率分别为96.04\%、91.97\%和89.58\%，关键事实召回率分别为92.37\%、96.64\%和92.98\%，未表现出随Token用量增加而同步下降的单调趋势。250K档只有2轮，虽然两项指标均为100\%，样本量不足以支持稳定性结论；500K档没有实际样本，因此仍需独立压力实验。现有错误更集中在时间口径错配、部分回答遗漏、模糊问题的上下文继承，以及证据不足时的回答边界处理。

分领域结果沿用总体轮次级路由正确率的统计口径，每个领域均以1,410个对话轮次为分母。若一轮在该领域中出现必要调用错误或遗漏，则该领域在本轮记为路由错误；其余轮次记为正确。同一轮可能同时影响多个领域，因此各领域错误轮次数不直接加总。

\begin{table}[htbp]
  \centering
  \caption{分领域工具路由结果}
  \label{tab:tool-results}
  \footnotesize
  \setlength{\tabcolsep}{2.5pt}
  \begin{tabular}{@{}C{.20\textwidth}*{4}{C{.17\textwidth}}@{}}
    \fintracetoprule
    \fintracetablehead{工具领域} & \fintracetablehead{评测轮次数} & \fintracetablehead{路由正确轮次数} & \fintracetablehead{路由错误轮次数} & \fintracetablehead{轮次级路由正确率} \\
    \fintracemidrule
    财务工具 & 1,410 & 1,358 & 52 & 96.31\% \\
    股权工具 & 1,410 & 1,403 & 7 & 99.50\% \\
    事件工具 & 1,410 & 1,381 & 29 & 97.94\% \\
    文档与研报工具 & 1,410 & 1,357 & 53 & 96.24\% \\
    \fintracebottomrule
  \end{tabular}
\end{table}

工具调用实验共记录570次逻辑调用。经大模型辅助复核，286次调用被确认为回答问题所必需，其中242次的工具、操作和关键参数均正确，必要调用正确率为84.62\%。在1,410个对话轮次中，1,293轮满足所有必要调用正确且没有遗漏必要工具，轮次级路由正确率为91.70\%。分领域结果用于定位不同工具领域对总体路由错误的贡献。两项指标均不将额外调用计入错误范围。

错误处置结果依据同一批次的运行轨迹统计，并按错误产生环节展开。只有该轮所有错误均得到妥善处理，才计入正确处置轮次。

\begin{table}[htbp]
  \centering
  \caption{错误处置结果}
  \label{tab:error-handling}
  \small
  \setlength{\tabcolsep}{4pt}
  \begin{tabular}{@{}C{.25\textwidth}*{3}{C{.21\textwidth}}@{}}
    \fintracetoprule
    \fintracetablehead{错误类型} & \fintracetablehead{错误轮次数} & \fintracetablehead{正确处置轮次数} & \fintracetablehead{错误处置成功率} \\
    \fintracemidrule
    工具执行错误 & 0 & 0 & / \\
    动作或参数校验错误 & 56 & 56 & 100.00\% \\
    大模型输出错误 & 158 & 145 & 91.77\% \\
    网络与基础设施错误 & 8 & 4 & 50.00\% \\
    \fintracetotal{总体} & \fintracetotal{214} & \fintracetotal{198} & \fintracetotal{92.52\%} \\
    \fintracebottomrule
  \end{tabular}
\end{table}

全量评测共执行1,410轮，其中214轮出现轨迹可检测错误，运行错误率为15.18\%；198个错误轮次在最终一次Agent运行内处理了全部错误类型，错误处置成功率为92.52\%，总体可靠运行率为98.87\%。分类统计中，大模型输出错误最为常见，动作或参数校验错误均在运行内完成修复；网络与基础设施错误的自动处置率相对较低，说明外部接口连续失败仍是影响系统稳定性的主要因素。15轮仅在后续重新执行后恢复，另有1轮只处理了部分错误类型，均未计入Agent自动纠错成功。

\subsubsection{运行效率与资源消耗}

为说明回答质量与实际运行代价之间的关系，端到端计时覆盖请求解析、规划、工具调用、证据审查和答案生成。平均Token是每轮主模型与规划模型输入、输出Token之和的算术平均值，平均成本则根据各模型的实际Token用量和调用价格折算。长上下文问答与工具调用的失败分别归入上下文继承、实体与时间解析、工具路由、参数生成、证据审查、答案生成和基础设施，以便将延迟或成本异常与具体工作流环节对应。

\begin{table}[htbp]
  \centering
  \caption{不同运行场景的效率与资源消耗}
  \label{tab:runtime-cost}
  \footnotesize
  \setlength{\tabcolsep}{2.5pt}
  \begin{tabular}{@{}C{.24\textwidth}*{5}{C{.13\textwidth}}@{}}
    \fintracetoprule
    \fintracetablehead{场景} & \fintracetablehead{样本数} & \fintracetablehead{$\mathrm{P}_{50}$延迟} & \fintracetablehead{$\mathrm{P}_{95}$延迟} & \fintracetablehead{最大延迟} & \fintracetablehead{平均Token} \\
    \fintracemidrule
    确定性直连 & 163 & 67.89秒 & 160.15秒 & 252.77秒 & 9,371.96 \\
    有界调查 & 54 & 155.68秒 & 357.23秒 & 553.53秒 & 36,883.80 \\
    多工具调查 & 115 & 178.96秒 & 435.55秒 & 646.46秒 & 40,042.33 \\
    证据不足或拒答 & 1,078 & 0.03秒 & 51.57秒 & 306.19秒 & 2,654.93 \\
    \fintracebottomrule
  \end{tabular}
\end{table}

确定性直连、有界调查和多工具调查的$P_{95}$延迟分别为160.15秒、357.23秒和435.55秒。多工具调查需要经历规划、证据复核和多次工具执行，耗时高于确定性直连；其中，本地SQLite查询和图搜索较快，主要等待时间来自规划模型、最终回答模型以及需要在线生成查询向量的文档检索。证据不足或拒答场景共1,078轮，合计调用工具38次，平均每轮0.04次，多数请求在工具执行前即完成能力边界判断。工作流设置了工具预算、重复动作校验和终止条件，可以限制无效循环；整体效率更容易受到外部模型接口网络延迟和响应稳定性的影响。模型调用成本将在按照实际计费单价折算后补充。

本批次共调用大模型1,725次，输入和输出Token分别为8,096,426和2,889,814。总计10,986,240 Tokens。该统计包含请求解析、规划、动作修复、证据审查、记忆摘要和最终回答等结构化推理环节。

\subsection{股权穿透与事件脉络}

\subsubsection{股权穿透完整严格准确率}

股权穿透的价值在于还原完整、可验证的控制或持股链条，任意一跳的主体、方向或比例错误都可能改变最终结论。因此，本文采用案例级完整严格准确率，而不以单个节点的局部正确代替整条路径的正确。评测范围从工具接收已校验的起点、终点和观察日开始，覆盖实体桥接、图查询、比例计算和结构化结果组装。参考路径由评测程序直接读取冻结股东快照和经确认的主体桥接关系，通过独立SQL和深度优先搜索重建，不调用被测路径搜索函数；随后由固定版本大模型复核主体语义和来源支持。一个案例只有在观察日、路径方向、每一跳节点、每一跳比例、累计比例和必要路径全部正确，且不存在无证据额外路径时才通过。

设有效穿透案例总数为$N_{\mathrm{path}}$，其中满足全部严格条件的案例数为$N_{\mathrm{strict}}$，则股权穿透完整严格准确率为：

\[
A_{\mathrm{ownership}}=\frac{N_{\mathrm{strict}}}{N_{\mathrm{path}}}.
\]

$N_{\mathrm{path}}$仅包含路径深度大于3且能够从冻结股东快照独立重建完整参考路径的案例。对于一条包含$L$跳的持股路径，其累计持股比例按每一跳比例$r_l$连乘计算：

\[
r_{\mathrm{path}}=\prod_{l=1}^{L}r_l.
\]

路径中的节点、方向或任一$r_l$错误，都会使该案例无法计入$N_{\mathrm{strict}}$；这与股权穿透结论必须逐跳成立的业务要求一致。

\subsubsection{事件节点与事件簇质量}

事件脉络实验复用专项性能实验中的30家公司及其冻结事件索引，不新增公司或合成事件。外部评审模型在不使用互联网和模型记忆的条件下，依据事件标题、摘要、公告文号、主体、阶段和日期，从708条既有事件中独立识别关键节点并划分参考事件簇。系统聚类结果不作为参考划分依据。该实验评价现有事件索引中的关键节点保留和脉络组织质量，不用于推断系统针对全部原始公告的事件抽取召回率。

设外部评审确认的关键节点总数为$N_{\mathrm{event\_reference}}$。其中，被系统事件脉络保留的节点数为$N_{\mathrm{event\_hit}}$，则事件节点召回率定义为：

\[
R_{\mathrm{event}}=\frac{N_{\mathrm{event\_hit}}}{N_{\mathrm{event\_reference}}}.
\]

该指标衡量关键事件在事件查询和聚类组织过程中是否被遗漏。由于参考节点从既有事件索引中选择，结果只说明索引内节点的保留情况，不能替代基于全部原始公告开展的事件抽取完整性实验。

节点召回率能够反映事件是否完整，却无法单独评价事件簇的边界。因此，实验同时检查聚类成对F1、日期顺序、知识截止日、事件阶段一致性和显式因果关系的来源支持。聚类评价将任意两个事件视为一个事件对：系统结果与外部参考划分均将其归入同一事件簇时计为真正例，据此得到事件对精确率$P_{\mathrm{pair}}$和召回率$R_{\mathrm{pair}}$，并计算：

\[
\mathrm{F1}_{\mathrm{pair}}=\frac{2P_{\mathrm{pair}}R_{\mathrm{pair}}}{P_{\mathrm{pair}}+R_{\mathrm{pair}}}.
\]

该指标同时惩罚错误合并和错误拆分。事件先后顺序与来源证据相反、将后续事件写为前因、混淆事件阶段或使用截止日后信息，均视为明显逻辑断裂，并在结果中单独统计。

\subsubsection{专项实验结果与工具性能}

工具执行效率从工具接收到已校验参数开始计时，到完整结构化结果返回为止，包含数据库查询、图搜索、计算和结果组装，不包含规划、最终答案生成和前端渲染。各工具操作在预热后至少运行30次，使用$\mathrm{P}_{95}$延迟判断是否满足5秒的竞赛要求。

股权穿透实验按路径深度分为两组：深度等于3层的路径作为中等深度对照组，深度大于3层的路径用于检验竞赛要求。后者从4至6层候选路径中分层抽样。每个案例的起止主体、路径方向、每一跳主体、持股比例和累计比例全部正确时，才计为严格通过。

\begin{table}[htbp]
  \centering
  \caption{股权穿透专项结果}
  \label{tab:ownership-results}
  \footnotesize
  \setlength{\tabcolsep}{2.5pt}
  \begin{tabular}{@{}C{.20\textwidth}*{4}{C{.17\textwidth}}@{}}
    \fintracetoprule
    \fintracetablehead{专项} & \fintracetablehead{样本数} & \fintracetablehead{严格通过数} & \fintracetablehead{完整严格准确率} & \fintracetablehead{$\mathrm{P}_{95}$延迟} \\
    \fintracemidrule
    深度等于3的持股穿透路径 & 30 & 30 & 100.00\% & 0.321秒 \\
    深度大于3的持股穿透路径 & 30 & 30 & 100.00\% & 0.336秒 \\
    \fintracebottomrule
  \end{tabular}
\end{table}

事件实验分别报告关键节点检索与事件聚类的结果。事件查询以节点召回率为核心指标，事件聚类以聚类成对F1为核心指标，两类操作均记录$\mathrm{P}_{95}$延迟。

\begin{table}[htbp]
  \centering
  \caption{事件脉络专项结果}
  \label{tab:event-results}
  \footnotesize
  \setlength{\tabcolsep}{2.5pt}
  \begin{tabular}{@{}C{.16\textwidth}*{6}{C{.12\textwidth}}@{}}
    \fintracetoprule
    \fintracetablehead{专项} & \fintracetablehead{样本数} & \fintracetablehead{关键节点召回率} & \fintracetablehead{聚类成对精确率} & \fintracetablehead{聚类成对召回率} & \fintracetablehead{聚类成对F1} & \fintracetablehead{$\mathrm{P}_{95}$延迟} \\
    \fintracemidrule
    事件查询 & 30 & 100.00\% & / & / & / & 0.008秒 \\
    事件聚类 & 30 & 100.00\% & 93.13\% & 32.34\% & 48.00\% & 0.007秒 \\
    \fintracebottomrule
  \end{tabular}
\end{table}

深度等于3层和深度大于3层的股权穿透实验各纳入30个现有案例，后者包含4层、5层和6层路径各10个。两组共60次工具调用均正常完成，$P_{95}$延迟分别为0.321秒和0.336秒。评测程序从原始快照独立重建参考路径，逐项核对路径数量、节点、方向、每跳比例、累计比例、日期和证据标识；60例确定性比较全部通过。固定版本大模型进一步复核主体同一性、关系语义和来源支持，两组也均为30例通过，完整严格准确率均为100.00\%。该结果说明工具在当前冻结主要股东快照和已确认主体桥接覆盖范围内能够稳定复现可证明路径，不代表未披露工商关系或非前十大股东关系也被完整覆盖。

事件查询与事件聚类分别完成30次受控调用，$\mathrm{P}_{95}$延迟分别为0.008秒和0.007秒。外部评审共确认117个关键节点，系统全部保留，关键节点召回率为100.00\%。聚类成对精确率为93.13\%，召回率为32.34\%，$\mathrm{F1}$为48.00\%。较高的精确率表明不同事件被错误合并的情况较少，较低的召回率则说明同一事件的发展记录经常被拆分到多个小簇，当前聚类规则偏保守。30个案例均未发现严重时序断裂或知识截止日违规。本批次没有生成显式事件关系，因此无证据因果关系计数为0，但该结果不能用于证明因果关系识别能力已经得到验证。

\subsection{财务风险预警与报告质量}

财务风险样本中，未触发风险的情形通常多于触发风险的情形，单独使用准确率容易被多数类掩盖。为同时约束误报与漏报，本实验采用F1作为风险预警的核心指标，并分别报告精确率和召回率。样本以“公司、目标报告期、风险类型”为最小单位，参考标签由固定版本外部金融评审模型依据冻结财务记录、规则适用条件和截止日前证据独立复算，只描述可观察的风险信号，不将规则命中直接解释为财务造假。

财务风险预警的精确率、召回率和F1定义为：

\[
P_{\mathrm{risk}}=\frac{\mathrm{TP}_{\mathrm{risk}}}{\mathrm{TP}_{\mathrm{risk}}+\mathrm{FP}_{\mathrm{risk}}},
\qquad
R_{\mathrm{risk}}=\frac{\mathrm{TP}_{\mathrm{risk}}}{\mathrm{TP}_{\mathrm{risk}}+\mathrm{FN}_{\mathrm{risk}}},
\]

\[
\mathrm{F1}_{\mathrm{risk}}=\frac{2P_{\mathrm{risk}}R_{\mathrm{risk}}}{P_{\mathrm{risk}}+R_{\mathrm{risk}}}.
\]

其中，$\mathrm{TP}_{\mathrm{risk}}$表示系统与外部参考标签均判定存在该类风险信号的样本数，$\mathrm{FP}_{\mathrm{risk}}$表示系统触发但外部参考标签为负的样本数，$\mathrm{FN}_{\mathrm{risk}}$表示外部参考标签为正但系统未触发的样本数。精确率反映误报程度，召回率反映漏报程度，F1是二者的调和平均值；只有同时减少误报和漏报，F1才会提高。

真正例、假正例和假负例由工具输出与外部参考标签逐项比对获得。数据不足和规则无法适用的样本单独核验状态识别，不进入风险正负样本分母；数据足以形成正向参考标签而系统未覆盖相应风险类型时计为假负例（FN）。正式结果采用冻结风险类型全集上的微平均F1，使每个样本对总体结果具有相同权重；各风险类型的精确率、召回率和F1用于呈现不同规则的性能差异。

规则命中正确并不意味着最终报告具有实际使用价值，报告还需要准确引用数据、解释指标关系并保持审慎的风险措辞。因此，财务排雷报告采用固定版本外部金融评审模型实施代理盲评，与确定性规则的F1形成互补。评审从数据与引用、逻辑自洽、金融专业性、完整与可用性四个维度评分，每项采用1至5分。四个维度均不低于4分、平均分不低于4.0，且不存在核心事实错误、虚假引用、截止日泄露或直接定性造假时，报告判定为优秀。该结果用于统一口径下的批量比较，正式专家验收仍应由具备财务、审计或证券研究背景的人员复核。

完成评分后，代理盲评优秀率按下式计算：

\[
E_{\mathrm{report}}=\frac{N_{\mathrm{excellent}}}{N_{\mathrm{valid}}}.
\]

其中，$N_{\mathrm{valid}}$为完成盲评且不存在样本损坏等排除情形的报告总数，$N_{\mathrm{excellent}}$为同时满足四项评分门槛且不存在否决性错误的报告数。该指标将多维评分转化为报告级验收结果，避免单项高分掩盖核心事实错误或虚假引用。

财务风险结果按风险类型分别报告正负样本规模、精确率、召回率和F1，并使用微平均结果反映整体预警能力。报告质量通过代理评审的评分均值、评分离散程度、优秀占比和否决性错误数量进行评价。

\begin{table}[htbp]
  \centering
  \caption{分类型财务风险识别结果}
  \label{tab:risk-results}
  \footnotesize
  \setlength{\tabcolsep}{2.5pt}
  \begin{tabular}{@{}C{.24\textwidth}*{5}{C{.13\textwidth}}@{}}
    \fintracetoprule
    \fintracetablehead{风险类型} & \fintracetablehead{正样本} & \fintracetablehead{负样本} & \fintracetablehead{精确率} & \fintracetablehead{召回率} & \fintracetablehead{F1} \\
    \fintracemidrule
    利润与经营现金流背离 & 11 & 5 & 100.00\% & 100.00\% & 100.00\% \\
    存货与收入背离 & 10 & 9 & 100.00\% & 100.00\% & 100.00\% \\
    应收账款与收入背离 & 4 & 16 & 100.00\% & 100.00\% & 100.00\% \\
    经营现金流持续为负 & 2 & 20 & 100.00\% & 100.00\% & 100.00\% \\
    销售收现与收入背离 & 10 & 12 & 100.00\% & 100.00\% & 100.00\% \\
    利润率异常变化 & 13 & 10 & 100.00\% & 100.00\% & 100.00\% \\
    流动性与杠杆压力 & 18 & 26 & 100.00\% & 100.00\% & 100.00\% \\
    \fintracetotal{总体微平均} & \fintracetotal{68} & \fintracetotal{98} & \fintracetotal{100.00\%} & \fintracetotal{100.00\%} & \fintracetotal{100.00\%} \\
    \fintracebottomrule
  \end{tabular}
\end{table}

风险预警实验从全量问答批次中取得24次已完成的财务风险扫描，按实际执行规则展开为179个“公司—期间—风险类型”判断。其中166个样本具备完整指标并能够形成正负参考标签，包括正样本68个、负样本98个；另有13个样本因数据不足或规则无法适用而排除在正负分类指标之外。系统在166个有效样本上未出现误报或漏报，总体精确率、召回率和微平均F1均为100.00\%。外部金融评审依据冻结财务指标、规则适用条件、计算公式和阈值独立复算参考标签，未接触系统原判定。

该结果表明风险扫描工具能够按照既定规则稳定复现跨期勾稽结果，但其评价边界是规则计算一致性，不等同于利用真实财务造假结果检验预测能力。经营现金流持续为负等规则所需字段较少，执行条件相对稳定；利润与现金流、存货与收入、应收账款与收入等跨期规则依赖连续可比期间，数据不足时会进入不可评价状态；利润率、流动性和杠杆规则仍可能受到行业差异及初始阈值适用范围的影响。因此，实验可以验证现有规则实现及证据链是否正确，\fintracecaution{不能据此声称}已经覆盖所有财务舞弊模式。

在风险识别结果的基础上，代理盲评进一步检验报告能否把指标、证据和风险解释组织成完整研判。除严格优秀率外，实验同时报告无否决性错误率，以区分“内容不够完整”与“存在严重事实或证据错误”。

\begin{table}[htbp]
  \centering
  \caption{财务报告质量评价结果}
  \label{tab:report-review}
  \footnotesize
  \setlength{\tabcolsep}{2.5pt}
  \begin{tabular}{@{}C{.20\textwidth}*{4}{C{.17\textwidth}}@{}}
    \fintracetoprule
    \fintracetablehead{报告质量指标} & \fintracetablehead{平均分} & \fintracetablehead{标准差} & \fintracetablehead{达标占比} & \fintracetablehead{否决性错误数量} \\
    \fintracemidrule
    数据与引用 & 3.92 & 1.11 & 58.33\% & 0 \\
    逻辑自洽 & 3.92 & 0.57 & 79.17\% & 0 \\
    金融专业性 & 4.21 & 0.50 & 95.83\% & 0 \\
    完整与可用性 & 2.75 & 0.66 & 12.50\% & 0 \\
    无否决性错误率 & / & / & 100.00\% & 0 \\
    \fintracetotal{总体优秀率} & \fintracetotal{/} & \fintracetotal{/} & \fintracetotal{12.50\%} & \fintracetotal{0} \\
    \fintracebottomrule
  \end{tabular}
\end{table}

有效盲评报告共24份，其中优秀报告3份，优秀率为12.50\%，未发现核心事实错误、无依据引用、知识截止日违规或直接定性造假等否决性错误。金融专业性平均得分最高，说明报告能够较好地区分风险信号与确定事实，并保持审慎措辞；逻辑自洽和数据引用总体接近良好水平，但不同案例之间仍有较明显差异。完整与可用性平均得分仅为2.75，是优秀率偏低的主要原因。现有回答大多以部分回答状态结束，面对“基本面怎么样”“有哪些风险”等宽泛问题时，往往只解释部分已取得证据，没有充分覆盖主要风险信号、跨工具信息及其对用户问题的综合含义。因而，后续改进重点不在增加结论强度，而在保证核心风险覆盖、组织跨工具证据，并明确区分已验证信息、数据缺口和进一步核查方向。

财务专项的运行效率分为两个环节记录。风险扫描是确定性计算过程，统计数据查询与规则计算耗时；报告生成依赖大模型，除延迟外还记录输入输出Token和调用成本。失败原因按指标数据、报告期口径、风险规则、证据引用和报告生成分类，用于解释异常耗时和报告质量变化。

\begin{table}[htbp]
  \centering
  \caption{财务专项运行效率}
  \label{tab:risk-cost}
  \footnotesize
  \setlength{\tabcolsep}{2.5pt}
  \begin{tabular}{@{}C{.24\textwidth}*{5}{C{.13\textwidth}}@{}}
    \fintracetoprule
    \fintracetablehead{环节} & \fintracetablehead{样本数} & \fintracetablehead{$\mathrm{P}_{50}$延迟} & \fintracetablehead{$\mathrm{P}_{95}$延迟} & \fintracetablehead{最大延迟} & \fintracetablehead{平均Token} \\
    \fintracemidrule
    风险扫描 & 24 & 0.007秒 & 0.020秒 & 0.024秒 & / \\
    财务报告生成 & 24 & 52.67秒 & 66.75秒 & 67.73秒 & 7,604.08 \\
    \fintracebottomrule
  \end{tabular}
\end{table}

在全量问答评测产生的24次财务风险分析中，风险扫描和财务报告生成的$P_{95}$延迟分别为0.020秒和66.75秒，报告平均消耗7,604.08 Tokens。风险扫描在本地完成数据查询和规则计算，耗时较低且波动较小；报告生成需要组织多条证据并调用主模型，是财务专项更主要的延迟来源。上述结果用于描述实际运行效率，不替代风险预警F1和报告质量复核；单份平均成本将在按照实际计费单价折算后补充。

\subsection{综合分析与本章小结}

前述实验分别检验了FinTrace的多轮问答与工具路由、股权和事件专项工具，以及财务风险识别与报告生成能力。将这些结果放回竞赛验收框架，可以更清楚地判断系统已经形成的能力、尚未达到的指标及其改进方向。下表沿用各分项实验的样本范围和计算口径，对九项技术要求进行统一对照。

\begin{table}[!ht]
  \centering
  \caption{竞赛技术要求综合验收结果}
  \label{tab:acceptance-results}
  \scriptsize
  \renewcommand{\arraystretch}{1.12}
  \setlength{\tabcolsep}{3pt}
  \begin{tabular}{@{}C{.37\textwidth}C{.20\textwidth}C{.27\textwidth}C{.08\textwidth}@{}}
    \fintracetoprule
    \fintracetablehead{竞赛技术要求} & \fintracetablehead{实验结果} & \fintracetablehead{验收目标} & \fintracetablehead{结论} \\
    \fintracemidrule
    0.5M Tokens、10轮以上问答准确率 & / & ≥90\% & \fintracestatuspending{尚待专项实验} \\
    0.5M Tokens、10轮以上事实召回率 & / & ≥90\% & \fintracestatuspending{尚待专项实验} \\
    外部工具轮次级路由正确率 & 91.70\% & ≥92\% & \fintracestatuswarn{未达到} \\
    轨迹错误自动处置率（自纠错成功率） & 92.52\% & ≥80\% & \fintracestatuspass{达到} \\
    深度大于3层的股权穿透完整严格准确率 & 100.00\% & ≥85\% & \fintracestatuspass{达到} \\
    事件关键节点召回及因果、时序一致性 & 100.00\%；无严重断裂 & 召回率≥85\%；无逻辑断裂 & \fintracestatuspass{达到（现有索引范围）} \\
    股权穿透与事件脉络工具执行时间 & 0.336秒；0.008秒 & ≤5秒 & \fintracestatuspass{达到} \\
    财务风险预警微平均F1 & 100.00\% & ≥85\% & \fintracestatuspass{达到（既定规则范围）} \\
    财务排雷报告优秀率（代理盲评） & 12.50\% & ≥80\% & \fintracestatuswarn{未达到，仍需专家复核} \\
    \fintracebottomrule
  \end{tabular}
\end{table}

九项技术要求中，五项已经达到当前验收目标，两项未达到，另有两项因0.5M Tokens专项压力实验尚未开展而暂不形成结论。现有1,410轮实验说明，分层记忆、实体解析和时间约束能够支持常规多轮任务，但实际单轮最高Token总量为116,283，不能据此推断极限上下文条件下的回答准确率和事实召回率。工具轮次级路由正确率为91.70\%，距离92\%的目标相差0.30个百分点；相比之下，真实运行错误的\fintracemetric{自动处置率达到92.52\%}，说明系统在参数修复、重新规划和安全终止方面已有较稳定表现。下一阶段需要优先减少复杂调查中的必要工具遗漏和关键参数错配，而不是继续增加额外调用。

专项工具的优势主要体现在确定性计算和执行效率。深度大于3层的股权穿透案例全部严格通过，股权与事件工具的$P_{95}$延迟也均低于5秒。事件实验则呈现出更具体的改进信号：索引内关键节点全部得到保留，且未发现严重时序断裂，但聚类成对$F1$仅为48.00\%，说明系统倾向于将同一事件链拆分为多个小簇。后续应重点加强公告文号、涉及主体、事件阶段和主题特征的联合匹配。上述结果仅覆盖前十大股东快照、已确认主体桥接和既有事件索引，不能外推到未披露工商关系、非主要股东关系或尚未抽取的原始公告。

财务实验进一步说明了“工具正确”与“报告优秀”之间的差距。风险扫描在既定规则范围内实现100.00\%的微平均F1，表明跨科目计算、适用条件和证据关联能够稳定复现；这一结果验证的是规则实现一致性，并非真实财务造假预测率。报告代理盲评未发现核心事实错误、无依据引用或越过风险语义边界等否决性问题，但严格优秀率只有12.50\%，主要受完整与可用性不足影响。系统下一步需要把已经获得的结构化信号转化为覆盖充分、层次清楚的研判，同时补充现有三大报数据尚不能支持的关联交易、异常费用和汇兑风险边界。

综合来看，FinTrace已经建立了可追踪的多轮工作流、低延迟的确定性专项工具和具备证据约束的财务风险扫描。当前短板也较为集中：0.5M Tokens条件缺少直接验证，工具路由仍有少量必要调用遗漏，事件聚类偏保守，最终财务报告的内容覆盖不足。后续工作应围绕这四项问题开展针对性实验和迭代，并继续保留主体、期间、证据与知识截止日的结构化审计记录，使性能提升能够被同一套评测口径复核。
